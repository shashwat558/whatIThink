// import { blogState } from '@/state/state'

// import { useRecoilValue } from 'recoil'
// import BlogCard from './BlogCard';
// import { Blog } from '@/types/types';

// const BlogList = () => {
//     const blogs = useRecoilValue<Blog[]>(blogState);
//   return (
//     <div>
//         {blogs.map((blog) => (
//             <BlogCard key={blog.id} title={blog.title} description={blog.description} publishedDate={blog.publishedDate} readingTime={blog.readingTime} />
//         ))}
//     </div>
//   )
// }

// export default BlogList